key: debian/dists/bullseye.gpg

source:
  upstream:
    url: https://debian.mirror.rafal.ca/debian/
    release: bullseye
    architectures:
      - all
      - amd64
      - arm64
    components:
      - main
      - contrib
      - non-free

# config for filter